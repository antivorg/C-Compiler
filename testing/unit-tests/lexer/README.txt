





# Stimulus

The stimulus is a sequence of valid tokens, consisting of 2 subsequences:
- 1 : A fixed sequence containing a single instace of each token type, each is parsed as if seperate files.
- 2 : A variable length sequence based of sets of multiple tokens, which are selected semi-randomly to adhere to gramar rules.

